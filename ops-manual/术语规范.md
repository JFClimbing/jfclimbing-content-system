# 术语规范（攀岩 · 三平台通用）

> **建立于** 2026-09-18 · **v2 修订**：改为「专业词汇一律联网验证」
> **起因**：连续出了两个术语错 ——
> ① 把你的「**向导环**」写成「导向环」
> ② 编了一个不存在的词「**女贞结**」（网上查无此词）
>
> **用户明确指示**：涉及专业词汇**直接联网搜索**。
> 她本人也可能记错 —— **不把任何人的记忆当权威，只认可查证的来源。**

---

## 一、铁律（v3：官方文档优先）

> **用户定调（2026-09-18）**：「官方的数据，这些官方数与那些大品牌的技术文档里面都有，
> 就用他们的就行了。」
> → **参数和术语，以品牌官方技术文档为准，不用问你、也不靠我记忆。**

```
① 【新】参数与术语，先去品牌官方技术文档拿 —— 这是第一顺位
② 来源优先级：品牌官方文档 > 厂商官方中文站 > 行业权威 > 用户原话 > 网络问答
③ 搜不到出处的词，不许写进交付物
④ 交付前跑一次全局 grep 复核
⑤ 术语错一次 = 专业信任度掉一层，这是不该由用户发现的问题
```

### 可直接访问的官方站点（2026-09-18 实测全部可达）✅

```
petzl.com          petzl.cn（官方中文）      edelrid.com
blackdiamondequipment.com        mammut.com
singingrock.com    wildcountry.com          beal-planet.com
theuiaa.org
```

> 写稿前遇到参数（kN / mm / 认证 / 长度），**先去这些站点拿数**。

### 验证流程（每次涉及专业词都走一遍）

```
1. 列出这篇稿子里的所有专业词
2. 逐个搜索：中文词 + 英文原词（如「水结 water knot」）
3. 能查到 ≥2 个独立来源 → 通过
4. 只查到我自己的说法 → 删掉或换词
5. 查到的和你说的不一致 → 摆出证据，你拍板
```

---

## 二、已联网验证的术语（**照这个写**）

| ✅ 用这个 | ❌ 别用 | 验证来源 |
|---|---|---|
| **向导环** | ~~导向环~~ | 你的内容库 12 处 + 知乎《手中的攀岩保护器》「向导模式」 |
| **释放环** | ~~释放孔~~ | 你的内容库 8 处 |
| **散扁带 / 快挂扁带 / 成型扁带** | ~~女贞结~~（无此词，AI 编的） | PEAK CLIMB《攀岩扁带大起底》：常用扁带三种 |
| **水结**（Water Knot，又叫扁带结 / 环固结） | — | 百度百科「水结」+ knots3d + 知乎绳结帖：连接两条织带末端 |
| **直门 / 弯门 / 丝门（金属线圈式）** | — | 百度百科「快挂」闸门分类 + 淘宝商品页 |
| **快挂 = 两个锁扣 + 一根连接扁带** | ~~主锁 + 扁带 + 下锁~~ | 百度百科「快挂」：由两个单向锁扣与扁带组成 |
| **龟龟**（= GriGri） | — | 你的内容库 28 处 |
| **结组** | — | 你的内容库 48 处 |
| **上方保护** | — | 你的内容库 25 处 |
| **主锁 / 快挂 / 扁带 / 保护器 / 下降器** | — | 你的内容库 46 / 28 / 20 / 11 / 3 处 |
| **管式（手动制动）/ 辅助制动（凸轮辅助）** | — | 你第 3 集定稿用词（行业里更常说"管状保护器 / 自锁保护器"，以你为准） |
| **五步保护法 / 8 字环 / 向导模式** | — | 你第 3 集定稿 + 知乎长文 |

---

## 三、已排除的错误词（留作黑名单）

| 错词 | 问题 | 怎么发现的 |
|---|---|---|
| ~~导向环~~ | 正确是「向导环」 | 用户指出 + 全库 grep 反查（她自己用了 12 次） |
| ~~女贞结~~ | **查无此词**，AI 幻觉 | 联网搜索 0 结果 → 已换成「快挂扁带」 |
| ~~释放孔~~ | 你自己的叫法是「释放环」 | 全库 grep（8 处） |
| ~~上锁 / 下锁~~ | 查不到作为快挂两端锁的通用说法 | 联网搜索无支撑 → 改用「两个锁扣」 |

> 📌 **黑名单规则**：一旦某词进了黑名单，全项目 grep 排查一遍，并记录到这里。

---

## 四、交付前自检（每次给稿前跑）

```bash
cd /var/minis/mounts/运营/content-lab
# 黑名单词扫描
grep -rn "导向环\|女贞结\|释放孔\|下锁" . | grep -v samples | grep -v "videos/2026"
```

```
□ 这篇稿里每个专业词，都能说出「谁验证过、在哪」吗？
□ 有没有哪个词，我其实是在"猜"？
□ 新出现的专业词，联网查过了吗？
□ 同一篇里的分类用词前后一致吗？（别管式/管状混用）
□ 你说的和你写的不一样时，我摆证据，还是我直接改？（→ 摆证据，你拍板）
```

---

## 五、维护方式

- **我发现的**：每涉及新专业词 → 联网验证 → 通过就进第二节，查不到就进第三节黑名单
- **你发现的**：你说一个词不对 → 我先联网核实，再全项目 grep，再更新本文件
- **每次给稿**：专业词必须已经过验证，**不许让你当校对**
